import { ROOMS } from "../../data/testData"
import { DEVICES } from "../../data/testData"

const initialState = {
    rooms: ROOMS,
    devices: DEVICES,
}

const deviceReducer = (state, action) => {

}