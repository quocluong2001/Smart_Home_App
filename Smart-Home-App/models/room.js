class Room {
    constructor(id, name, imageSource, devices) {
        this.id = id
        this.name = name
        this.imageSource = imageSource
        this.devices = devices
    }
}

export default Room