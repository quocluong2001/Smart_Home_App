class Device {
    constructor(deviceId, type, name, status) {
        this.deviceId = deviceId
        this.type = type
        this.name = name
        this.status = status
    }
}

export default Device